import { OpTransformPipe } from './op-transform.pipe';

describe('OpTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new OpTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
